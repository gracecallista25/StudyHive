# StudyHive environment configuration
# No environment variables are defined yet.
# Add required variable names and safe example values when services are configured.
# Never put real secrets in this file.
