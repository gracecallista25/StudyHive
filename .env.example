# Optional: copy to .env.local and set your teammate's API URL, then restart Vite.
# Leave blank for frontend preview. This is public configuration, never a secret.
VITE_API_BASE_URL=
# Example local FastAPI URL: http://127.0.0.1:8000
